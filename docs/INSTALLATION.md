# 安装与权限

本项目使用 `planning/current/` 唯一规范，四个项目 Skills 已安装。稳定原档与共享数据位于本机安装配置指向的 `.local/store`、`.local/data`；临时 worktree 不持有唯一证据。

现场核验：macOS 27.0 / arm64；ChatGPT desktop 26.915.31945 (9922)；Node 22.22.2、npm 10.9.7、Python 3.9.6、Git 2.54.0。官方应用本任务可运行，GitHub 已登录；未读取凭据正文。现有模型默认是 `gpt-6-astra / ultra`，长期任务沿用实际用户默认，没有升级其他任务。

四项原生任务已通过官方管理工具创建、受控安全试跑并启用，现回读均为 ACTIVE；本机回执包含真实 ID、正文/配置哈希、绑定目录和实际设置。手动试跑由当前官方应用角色/子代理完成，并非原生 Run now。不得把安装或手动试跑当作自然运行证据。

计划采用本机 Asia/Shanghai：发现 07:30/19:30（原建议 Asia/Tokyo 08:30/20:30），研究每小时 05 分，日复核 08:15（东京 09:15），发布每小时 35 分。官方工具未返回实际下一触发，字段为 UNKNOWN，不能以计算值代替调度器回读。用户后续修改频率优先。

权限有三层：角色业务约定、程序的目录/所有权/哈希/不可变提交检查、实际应用环境。现场继承的默认权限是 full access / never；未修改全局设置。故当前不能声称每角色有只读原件挂载，也不能声称 GitHub 凭据只限一个仓库。发布入口核验唯一允许 remote，提交入口验证角色并拒绝旧原件变化；拥有同用户任意 shell 权限的进程仍可绕过入口，属于明确弱隔离。

官方说明定时任务继承默认 sandbox；程序权限不能冒充硬沙箱。来源：[官方定时任务文档](https://learn.chatgpt.com/docs/automations?surface=app)。

Computer Use 不允许操作 Codex 应用自身；创建/回读/启用仍可通过官方管理工具完成，安全试跑使用当前官方应用会话及子代理。没有可用 Run now 工具时，不伪造原生手动触发 ID。精确下次触发及自然触发待官方运行身份验证。

本任务另有每两小时的原生 heartbeat，只做剩余自然验收与必要修复接续，不替代四个业务任务，不补跑、不改频；状态无变化时保持安静。四任务当前状态见 `config/native_task_status.json`。
