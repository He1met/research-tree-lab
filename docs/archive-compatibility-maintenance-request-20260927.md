# 决策原件归档兼容性：独立维护提议

2026-09-27 发布观察发现：完整提交记录 `decision-numeraire-boundary-20260927` 的 `future_child`、`new_feedback_successor_created`、`trial_state` 不在现行公开白名单。固定公开投影正确省略这些字段；固定 `export_backup` 拒绝把省略后的副本称为精确原件备份，错误为 `Original has non-whitelisted fields; cannot claim exact public backup`。

本次处置：不改原件、不拓宽白名单。其余4条新增原件及1条产品依赖正常归档；该决策公开投影已发布，完整原件仍未远端备份。生产原件79条全部校验通过，属于公开导出兼容缺项，不是研究失败。

建议独立维护版本审查这三个字段的语义、递归公开安全及兼容性，再决定是否允许精确公开归档。验证须覆盖嵌套敏感内容拒绝、未执行后继不被图化为已执行、试行状态不升级经济/自然验收、旧快照及原件字节不变。未独立批准安装前保持缺项；若批准，仅补归档与必要发布，不重做研究。

本提议未实施公共schema、依赖、评价器或安全边界变更，也未触发其他任务。
