# Versioned exact-original archive permission v1

This maintenance candidate permits one already-authored decision original:
`decision-numeraire-boundary-20260927`, canonical SHA-256
`6b5ec5278a5d5bdc3945d483035a4d6c9e1f9ffad8871bf71933ce109aa7c64c`.
It does not expand the generic public projection or record schema.

The three omitted fields describe a proposed, unexecuted child, the absence of
a newly created feedback successor, and `PREPARATION_ONLY`. Their complete
original bytes were inspected. The archive gate checks the exact original hash,
record identity, public own-analysis disclosure, the exact three additional
keys, the closed child-object shape, matching parent, bounded string question,
`PROPOSED_NOT_EXECUTED`, strict boolean false and `PREPARATION_ONLY`.

This is a source-versioned single-original export permission. No installation
metadata, caller parameter, input file, hash list or record claim can grant it.
Any change to the original, its base projected fields, question, identifiers,
status or disclosure requires a new independent maintenance review. Explicit
`disclosure.export_fields` always excludes this exception, even when its list
claims all fields. Other omitted fields and record kinds remain rejected.

The complete original still passes the existing recursive content scanner.
The archive manifest marks only this entry with
`exact_export_policy=EXACT_NUMERAIRE_DECISION_METADATA_V1`. Inspection and restore
recheck the same permission and marker; recomputing archive hashes cannot grant
permission. The marker records byte-export scope, not economic validation or
natural execution. All nine existing archives were inspected read-only without
changing bytes and passed the new checks.

Only `researchlib/archive.py` changes in production code. `public.py`, snapshot
generation and graph rules are untouched. The old 16-source method and current
19-source formal method identities must remain identical. The website continues
to omit these three fields; the proposed child cannot become a graph node and
the trial cannot become active through this archive-only permission.

Engineering validation includes the exact original fixture, negative mutations,
explicit field restriction, altered policy markers, and real original read-only
export/inspect/restore in a disposable directory. The real closure contains
18 records and 41 files; all restored bytes matched, 79 pre-existing originals
and the projection/graph digest remained unchanged. Temporary outputs were
discarded. This is not official catch-up archiving, remote upload, scientific
reproduction, profitability or natural acceptance; the existing native publisher
must perform any authorized production catch-up after independent installation.

The original decision fixture is for tests only; production never reads it.
No formal-v4 source or frozen receipt, production original, task configuration,
installation or operational maintenance state was modified in this candidate.
