# 剩余验收原文与真实证据再审

本次只读再审认为：A07、F06、F08 已有满足原文的**真实、有界实例**，可依现行 `REAL_PASS` 定义更新其已验证范围；不应为通过这三项要求实际重启失败研究、耗尽额度或制造失联。未发生的异常分支仍不得记为实测通过。本文不修改验收索引，也不宣称 59 项全部完成。

## 范围与依据

- 规范：`planning/current/docs/ACCEPTANCE.md`，结合 `PROJECT_SPEC.md`、`RESEARCH_PROGRAM.md`、`AUTOMATIONS.md` 的相关条款；以原文为准，不以当前索引的下一动作倒推新增要求。
- 本次实际严格读取稳定 Store：54 条完整原件、52 PUBLIC、2 LOCAL_ONLY、8 个轮次，无异常。核对相关完整 decision/round/run、报告与已有独立验证，不新增行情或科学计算结果。
- 当前安装源码为 `eca93fe70146b1c6962842cd6b62dc8f66f89a47e8249017387202e1a6985277`。当前发布证据为快照 `86fef2218c8b65c855b304d7fbef4878e991282ecf5da78efed3228ac4d56263`，52 条公开记录、8 轮、92 个 HTTP 资源；这是既有发布回执的核对，本次未重新操作网站。
- 本次通过官方 `read_thread` 直接读取第三研究和第三发布任务的完整一轮记录。两者均 `completed`，各只有一条初始 Automation 用户消息；未发送消息、触发、停止或重启任何任务。任务身份留在官方工具回执，本报告仅保留脱敏摘要。
- 当前索引 59 个 ID 唯一、状态计数相符，所有证据路径存在。路径存在不等于重新执行全部测试；本次未重跑全套工程验收。

## A07：限定失败与重启条件已经存在

原文：“暂停范围准确，留下新证据重启条件，不推广到所有市场”。它要求**留下条件**，没有要求条件已经满足并实际重启。当前索引“满足新证据重启的完整实例”增加了门槛，且遗漏后续真实证据。

直接实例为 `r-btc-eth-paired-funding-20260923`、`run-btc-eth-paired-funding-20260923@attempt-1` 与 `decision-paired-funding-boundary-20260923`。研究保留三个同步结算点、双向持仓与不持仓、全部八组冻结摩擦情景。BTC 空 / ETH 多的资金费分量为双腿总名义的 0.6466188239855 bps；每次执行 0.5 bps 费用、零滑点情景后为 −0.3533811760145 bps。正方向的资金费分量不能覆盖任何已冻结的非零摩擦情景。已有独立分数算术复算核对三事件及全部 24 个候选/情景单元。

decision 明确 `NO_PLAN_SELECTED_LIMITED_NEGATIVE_COST_RESULT`，限定于已暴露三事件样本及固定分量/摩擦模型；禁止改名或事后挑便宜情景重选赢家。重启条件是新、未暴露、同步的 BTC/ETH 资金费和目标价格/mark 数据，以及完整数量、库存、进出场与费用规则。round 的下一动作同样冻结该有限负结果、等待上述新证据。实际后续研究转向进入确认、库存归因，并未把同一样本换名追认为配对策略获利。

**建议：A07 → `REAL_PASS`，范围限真实开发样本中成本分量假设的有限否定、暂停与可核验重启条件。** 不声称真实账户亏损、所有市场失效、整个可执行策略失败或已经重启。库存轮次“期末残余库存导致翻亏”在该样本并未出现，不能把它另算为已发生的失败实例。

## F06：实际局部阻塞没有成为全局停止

原文：“单项权限、配额或数据问题不让无依赖研究全部停止；没有额度不推算余额或无限重试。”

实际证据包括：

1. 旧 BTC 计划缺必要目标路径、mark 和完整经济输入时，独立股票机制调查、配对资金费分量、进入确认和库存归因继续完成；其结论保留开发样本与未知经济结果边界。
2. 首次发布的官方浏览器文件选择器明确拒绝权限，归档留在已验证本机状态；随后仅对同包作受支持的上传补发。下载受到浏览器策略拒绝后没有绕过，该阻塞未阻止第二、第三研究完成。
3. 第二、第三发布遇到 Mac 锁定，三包上传继续待办；第三研究仍完成，第三发布继续完成 Pages、92 资源验证、本机归档及 CI。锁屏导致原生 UI 不可用，不能泛称整台 App 已停止。

对应证据：三份 `docs/*native-research-completion.json`、三份 `docs/publication-run-20260923-v*.json`、`docs/archive-upload-recovery-20260923.json`，以及本次直接读取的第三研究/发布任务过程。原阻塞回执仍保留，不被后来的部分成功覆盖。

**建议：F06 → `REAL_PASS`，范围限已发生的单项数据不足、上传权限和 UI 环境阻塞不阻止无依赖工作。** 额度耗尽及所有其他权限种类没有实际发生，分别保留 `CONDITIONAL_NOT_TRIGGERED`，不称全错误类型都已测试。当前索引仍写“未覆盖原生权限失败”，已经遗漏实际权限拒绝证据。

另须纠正范围：`AUTOMATIONS.md` 第 6 节明确整体配额不足时本地观察也可能不能运行。因此不能把 F06 加严为“耗尽整个账户额度后其他模型任务必须继续”。不得为了补证故意耗尽额度、猜余额或反复重试。

## F08：真实长任务持续推进已直接观察

原文：“长任务有实际进展继续；失联只调查确认的本项目对象，未有控制能力不假称重启。”

本次官方任务回读直接确认：

| 实际任务 | UTC 起止 | 官方时长 | 可核对的进展 |
|---|---|---|---|
| 第三研究 | 2026-09-22 21:05:16 → 21:22:26 | 1,029,892 ms | 读取旧反馈与批准版本；固定库存方向和已暴露样本；独立复算成本；核对未获支持的残余库存假设；受控提交；回读 54 条原件 |
| 第三发布 | 2026-09-22 21:35:46 → 21:53:14 | 1,047,886 ms | 核对批准代码与新增原件；生成并推送快照；HTTP/浏览器核验；锁屏上传受阻后保留待办；完成本机恢复、独立收尾和 CI |

这些记录既有阶段说明，也有相应计算、提交、校验和发布工具执行，不是仅靠“还在运行”的心跳。两任务均没有第二条用户“继续”消息；其完整完成不依赖本次监督审计干预。

**建议：F08 → `REAL_PASS`，范围限有实质进展的真实长任务持续至完成。** 未观察到真正失联；失联对象确认与控制分支仍 `CONDITIONAL_NOT_TRIGGERED`。没有重启证据，也没有声称已经重启。原文不要求实际控制/恢复演练，当前索引“未实测长任务进展保持”已过时，“实际控制结果”不能成为必须制造的事件。

## 自然身份与其他条款

| 条目 | 再审结论 |
|---|---|
| A08 | 建议 `REAL_PASS`，限定上述官方任务从初始化到提交/发布没有普通用户接续审批；无需把 F07 全部自然证据作为 A08 的前置门槛。系统流程没有读取网页访问状态或等待用户回复才能继续的依赖，因此无需监控用户私人浏览行为来验收这一点；本次也未做此类调查。 |
| F07 | 原文没有规定必须取得名为 `scheduler-event ID` 的字段；可靠官方自然来源的等价证据也可接受。本次返回仅有 Automation 关联头、开始/完成与单初始消息，未暴露定时/手动触发类型；不凭时刻吻合推断。当前“严格自然研究 0”应读作**已核实计数为 0，实际来源 UNKNOWN**，不能解释为证明自然执行实际为零。自然日复核仍未发生，整项继续等待；既有真实反馈后继不需要重做。 |
| F01 | `AUTOMATIONS.md` 明确要求记录真实值或不可观察字段，下一触发可为未知。保留 native 时区/下一触发 UNKNOWN 是已知限制，不能用宿主 Shanghai 时区代证，也不能另要求一个官方未暴露的字段才承认真实安装。 |
| F02 | 管理不可用分支未发生，维持条件未触发；不必制造管理故障。 |
| F03/F04 | 原文明确接受如实披露弱隔离并以程序拒收越界结果。现有弱隔离实测不是强隔离证明，也不自动产生“必须扩权/改全局沙箱才能完成”的额外要求。 |
| F09 | 建议 `ENGINEERING_PASS`，限 36 小时持续 200、刷新不续期的两浏览器陈旧判断实测，以及本机依赖/不远程自救边界。整机/App 实际停止仍 `CONDITIONAL_NOT_TRIGGERED`，锁屏证据不能代替它。现有工程已验证的行为与未发生条件分开；原文没有“离线到恢复”全程演练要求，不应为转绿强行停机或重启。 |
| E12 | 当前无自动清理器、有显式闭包归档且未递归恢复目录；不必为验收新增危险的删除器。不称未执行的 GC 测试已通过。 |
| C09 | 新快照刷新保持选中/视窗与硬重载后抽屉持久化不是同一条件；第三发布的硬重载关闭抽屉不推翻第二发布的实际快照刷新保持证据。建议证据列表补入 v2 回读，而非新增产品要求。 |
| B04/B05/B07 | 条件计算、前驱私有链核验、等待迁移及输入解码已实现；正式来源覆盖判定、精确 mark 路径和真实经济终态接口仍有实现缺口。三项不能因本次运行验收建议升级而关闭，也不能把缺实现统称等未来数据。 |

其余条目本次仅做原文、状态和证据目录的一致性审查，没有发现足以据此推翻已有限定结论的新矛盾；不等同逐行为重测。59 项不能按单一绿色比例代表工程、自然、经济或恢复全部完成。当前本机允许公开归档覆盖 52/52，与远端 35 条摘要确认、26 条下载恢复仍是不同范围。

## 交付边界

本次只新增本报告及同名 JSON 回执；没有修改索引、规范、源码、原件、任务、安装或 Git。JSON 绑定当前索引、规范、实际证据文件和相关原件 hash，并保存官方任务回读的脱敏结论。后续可据此修正索引的已验证范围与未发生分支，不需要运行新的研究、补做失败或改写原运行身份。
