当前结论是 `CHANGES_REQUIRED / FORWARD_EVALUATOR_IMPLEMENTATION_REQUIRED`。两份 BTC 原方案的规则文本和手动生效前复核已经封存，但没有把未来官方数据按这些规则转换为成交、资金费、费用、权益路径和最终结果的固定评价器。数据即使随后齐全，现有账本与状态器也不能直接完成原规则复核。原件与既有工程测试仍有效；“等待未来窗口”“当前资料未核实”和“评价实现尚缺”必须分开记录。本次只读生产与源码，只新增本审计目录的脱敏材料，没有写新研究、评分或行情。

# 已核实的当前身份

审计时点为 `2026-09-22T18:52:30Z` 附近，仍早于两原方案 `2026-09-23T00:05:00Z` 的生效时点；原终点评价为 `2026-09-24T00:06:00Z`。

| 原方案 | canonical SHA-256 |
|---|---|
| `p-btc-funding-long-control-20260923@1` | `a8dd9a25bd4a5353bc8ed479ec41056bcf07704fccb285f2d014a9615358d02c` |
| `p-btc-funding-short-20260923@1` | `65ff62c3603d257b82b3274785fd1bfe2c5a784d215786be4775fe7fa076bce4` |

生产库完整 manifest 校验无异常，共 27 条记录；其中 `method=0`、`dataset=0`、`plan=2`、`review=2`。数据根目前保存此前官方下载的资金费 ZIP 与官方说明，未登记目标成交或标记价数据集。

每份计划的以下四个字段经实际 `Store.resolve_evidence` 检查均返回 `Unresolved evidence reference`：`method_ref`、`cost_model_ref`、`fill_model_ref`、`evaluation_contract_ref`。其中 `method_ref` 和 `evaluation_contract_ref` 都指向同一文本标签 `funding-cost-counterevidence-v1`，另外两个为 `funding-cost-counterevidence-v1-costs` 和 `official-tick-first-within-minute-plus-adverse-scenario-v1`。它们是已封存协议标签，目前不是已提交的可执行方法原件。

方法及程序搜索限于本项目 `researchlib/`、`scripts/`、`research/`、`review/` 和项目 Skill；不读取凭据、其他项目或交易账户。没有找到通用账本之外的生产 `replay_events(...)` 调用者。

# 具体阻断

| 级别 | 发现 | 当前证据与影响 |
|---|---|---|
| P1 | 冻结协议尚无可执行解释器及输入适配器 | `researchlib/replay.py:1-5` 明确要求上游提供已经按规则产生的事件。其函数不读取 plan，也不选择入退场、计算资金费事件或验证官方数据完整覆盖；当前生产调用链没有完成这些步骤的函数。完整行情到来仍然缺实现。 |
| P1 | 正式复核方法仅支持生效前状态 | `review/bootstrap-review/REVIEW_METHOD.md:3-9` 明确排除路径/收益评价；`prepare_review_bundle.py:35-36` 仅允许 `cutoff < effective_from` 且无旧 review。两条正式 review 全部绑定该状态方法，不能在原生下一次触发时把同一方法名称用于经济评分。 |
| P1 | 既有状态与通用账本缺迁移契约 | 两条真实 `simulation_state` 都是 `NOT_STARTED_BEFORE_EFFECTIVE` 状态摘要；直接以该原状态、空事件调用当前账本，都返回 `KeyError: 'cash_excluding_costs'`。未提供任何新行情或成交。这证明缺少明确的初始化/适配步骤，不是可以逐日直接传回的 ledger state。 |
| P1 | 计划特定费用、轨迹指标和终态程序尚缺 | 账本 `replay.py:63-94` 接收外部已经算好的 fee/funding amount，仅返回最后权益，不保留逐事件完整权益轨迹、不计算已实现/未实现分解、持有期、回撤或 12 个费用情景。`cost_stress.py:2-17` 是静态名义价值的历史费率条件算术，不能替代两方案的真实路径评价。 |
| P2 | 批次/FINAL 门禁不等于事件回放 | `review.py` 只冻结清单、判断复用并核对覆盖；`contracts.py:140-144` 检查原终点和 `data_complete=true`，不会从真实输入重建完整性。数据完整和最终状态仍需新评价器据证据计算，不能由填写 JSON 来证明。 |

以上不要求替换已经有效的存储、公开投影、React Flow、原生调度或角色控制。已有账本提供 Decimal、事件去重、时点拒绝、未知费用保留、库存及累计资金费等可复用基础，但不应单独宣称 B04/B05/B07 的真实策略执行已实现。

# 输入与时间边界

| 类别 | 具体缺口 | 应有状态 |
|---|---|---|
| 已可着手、目前未齐 | 官方成交/标记文件实际格式、排序依据、允许来源、完整性证据；精确结算标记能否从允许来源取得；公开费用资料与适用范围；数据与方法的受控登记 | `INPUT_CAPABILITY_UNVERIFIED` / `METHOD_IMPLEMENTATION_REQUIRED`，可在未来窗口之前核实与实现，不应只等待自然时间。 |
| 必须等待发生 | 原入场与退出 1 分钟窗口内的目标成交、持有期实际结算费率/结算标记和随后标记路径 | 现在为 `NOT_YET_EFFECTIVE`；发生后尚未取得或不完整则 `WAITING_DATA` / `MISSING_DATA`，不能倒签或制造。 |
| 项目不评估 | 用户实际费率等级、账户资格、订单实际滑点或成交 | 继续未知。公开费率表或假设滑点不得改称用户实际成本；原计划的 12 个显式费用情景可以独立计算并明确假设。不得为了得到“实际净收益”接账户或下单。 |

特别要区分“未来结果路径没有发生”和“当前执行事实不足”。两旧版本已冻结为 INCOMPLETE_DATA，它们仍应按旧规则复核；若未来要为当前新方案赋予执行资格，应按现有库约束登记新的计划版本和合法窗口，不能改写旧版本或把绿色标志当成复核前提。

原方案已明确真实费用未知时 `actual_net=null`。完整成交/标记/资金费路径到来可以让某些情景指标可计算，但不能据此自动认定实际成本完整。若原方案要求的必要输入依然缺失，总体状态仍须保留缺项；不能在评价器维护中放宽 FINAL 条件。若将来要改变终点或采用仅情景终态，则应提出新研究/方案版本，不能追认旧成绩。

# 最小合规维护版本

以下是一项当前可执行的维护范围，不是新策略、不改变两份原方案规则，也不是要求用户重新选择普通参数。

1. 新增一个**针对已封存协议的固定适配器**，由原 `method_ref` 与原 `plan_hash` 选择，读取真实 `instrument_ref`/数量/方向，不创建通用 Agent 平台或扩写四产品策略。未知协议拒绝评分。绑定源码、依赖、配置、输入 schema 和独立检查回执；用新的 `review_method_ref/evaluator_version`，记录真正实现时点。旧协议标签、旧 plan 和旧 review 均不改，不伪称新代码在原封存时已存在。
2. 增加合法官方文件的严格离线解码与受控数据登记：原字节哈希、合约、数据角色、单位、事件时点、实际取得时点、排序/交易 ID、覆盖区间、缺口及许可分开。完整首笔成交必须有覆盖和顺序依据；同毫秒多笔无可靠先后则报告 `PATH_AMBIGUOUS`，不能择优。只能取得 OHLC 时不能假称完整 tick；没有精确结算标记时不能插值成已知现金流。
3. 程序化执行原规则：只使用已封存的历史资金费信号，信号年龄按原予定入场时点计算，不按后来的复核日计算，也不重新换用新费率；入场/退出均遵守原 UTC 窗口；最多一次进入、一次退出、不再进入。完整路径确证与文件缺失分别判断，不把缺文件记为“未触发”。
4. 将每个实际结算事件变成 `direction × q × settlement_mark × realized_rate` 的费用事件，和账本“正值支付、负值收入”的语义对齐。按原 4×3 组合同时计算 12 个费率/滑点情景；实际成本单独保留未知。以原未调整交易价计算价格损益及既定费用公式，滑点只扣一次；不能同时用不利调整成交价又重复减同一滑点费用。
5. 明确状态转换：旧生效前状态经检查仍无事件/持仓后才能初始化新 ledger；初始化不改变旧原件，下一 review 仍绑定旧 `opening_state_hash`，记录适配方法版本和来源。随后保存事件游标/哈希、持仓、入场、已付费用/资金费、各情景状态及完整权益轨迹；每天截止不退出、不重进、不重计事件。补数或修订按新 review revision/原因处理。
6. 由程序产生毛价差、资金费、双边费用/滑点、实际未知项、情景净额、已实现/未实现、持有期、观测路径回撤及同窗口对照。两个方向结果不加成账户收益，不把相反 beta 的差异全归为资金费预测能力。明确区分 review 信息截至和可评价行情截至：后来合法取得的历史路径可用于后来复核，但实际取得时点不得倒签，更不能进入原事前信号。
7. 加一个固定的本地 review 执行入口，接收 frozen batch/原 plan ref/输入 manifest/旧状态，输出确定性 review/feedback outbox，再交已有受控提交入口；它是官方应用内计算助手，不是独立 Codex CLI、模型 API、行情 API 或替代调度器。现有 daily-review Skill 能调用这一入口，但自然触发身份仍须单独取证。
8. 独立验证后提交维护版本并更新批准源码哈希/实际 release 绑定。尽可能在原生效窗口前完成；若实现/验证晚于窗口，保存真实方法可得时间及数据曝光，不倒签。未知部分继续等待，不靠新方法名称掩盖事后改规则。

不必为此清空项目、改页面、重建原生任务、修改全局权限、迁移原始记录或重抽研究。方法/资料注册可作为新的维护原件加入，并由新的正式 review 引用旧 plan；不得在旧计划上补写“当时已经存在”的方法或数据链接。

# 维护验收应证明什么

- 冻结方案逐字段绑定；首笔选择具有完整覆盖依据；窗口边界、重复事件、缺资料与同时间顺序不明分别拒绝或保留未知。
- 正负资金费、双边费用、滑点只记一次；12 个情景全部输出；缺真实成本不产生实际净收益。
- 一次完整路径与分日接续的最终状态/各指标一致；第二天不重进、不重计；从现有两份生效前状态可透明迁移并保留旧状态哈希。
- 完整权益路径中间下探真实影响回撤，不仅比较开平两点；期末、未触发、无合法成交、缺结算标记分别处理。
- 输入/方法未变时重复执行幂等；输入修订有新版本和旧结果保留；代码、输入、输出可以在新目录复算。合成契约测试与真实路径验收仍分开。

这些维护检查现在即可开发与独立验收。真实未来路径、自然运行和经济效果仍须其各自时间及数据证据；它们不能代替这个实现缺口，也不需要等窗口到来才开始修复。

唯一下一动作：按以上小范围建立并独立冻结真实前向协议评价器；在此之前，将经济复核执行能力明确标为 `IMPLEMENTATION_REQUIRED`，原生任务可以如实提交等待/缺项记录，不能用状态器的成功代替策略评价完成。
